# Foundry surface-2 provider configuration (register #39/#47).
# Copy to .env (gitignored — NEVER commit real keys) and fill in.
#
# Development: xAI API with a PREPAID, HARD-CAPPED key (~$25 covers the whole
# dev phase — register #47). Production candidate: a local vLLM/xgrammar
# endpoint swaps in behind the same base URL with zero script changes.

FOUNDRY_DISCOVERER_BASE_URL=https://api.x.ai/v1
FOUNDRY_DISCOVERER_API_KEY=xai-your-prepaid-capped-key
FOUNDRY_DISCOVERER_MODEL=grok-4.3
